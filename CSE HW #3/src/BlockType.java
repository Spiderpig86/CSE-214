/**
 * The enum that specifies what character is parsed when reading through
 * the code. A brace is associated to "{" and "}", a paren is associated with
 * "(" and "}", and for is associated with the for loop syntax (the "for"
 * keyword).
 *
 * @author: Stanley Lim
 *      Email: Some Email
 *      Stony Brook ID: Some ID
 */
public enum BlockType {
    BRACE,
    PAREN,
    FOR
}
